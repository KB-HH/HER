export type Recipes = {
    id?: string,
    cookingtime: number,
    title: string,
    ingredients: string,
    unit: string,
    quantity: number,
    method: string,
    description: string,
    author: string,
    url: string,
    categories: string
}